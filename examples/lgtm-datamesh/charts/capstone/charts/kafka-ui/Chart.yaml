apiVersion: v2
name: kafka-ui
description: Kafka UI (kafbat) for the §17 capstone — a data-mesh infrastructure console to browse Kafka topics, messages, consumer groups, and (via Apicurio ccompat) schemas.
type: application
version: 0.1.0
appVersion: "latest"
